
interface Theme {
    colors : {
        nodeBackground: string;
        nodeHighlight: string;
        pathHighlight: string;
        strokeColor: string;
    }
}
export const theme: Theme = {
    colors: {
        nodeBackground: "#efefef",
        nodeHighlight: "#ffefb0",
        pathHighlight: "#d7a400",
        strokeColor: "#333333"
    }
}